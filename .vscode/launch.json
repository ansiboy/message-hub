{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/out/cli.js",
            "args": [
                "start"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}