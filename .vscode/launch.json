{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/cli.js",
            "args": [
                "start"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}